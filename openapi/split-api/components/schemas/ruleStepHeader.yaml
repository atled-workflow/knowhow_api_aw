type: object
pattern: rule.RuleStepHeaderModel
properties:
  name:
    description: ステップ名称
    type: string
  type:
    description: |
      ステップ種別<br>
      |パラメータ|説明|
      |----|----|
      |START|開始|
      |CREATE|作成|
      |APPLY|申請|
      |APPROVE|承認|
      |AUTOAPPLY|自動申請|
      |OUTPUTDOCDATA|自動出力|
      |CONFIRM|報告|
      |READ|閲覧|
      |STORE|保管|
      |SWITCH|分岐開始|
      |SYNC|分岐合流|
      |DISTRIBUTE|並列開始|
      |COLLECTION|並列合流|
      |END|終了|
    type: string
    pattern: rule.EnumRuleStepType
  index:
    description: インデックス
    type: integer
  code:
    description: ステップコード
    type: string
