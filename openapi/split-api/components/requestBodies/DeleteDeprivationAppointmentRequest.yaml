content:
  application/json:
    schema:
      required:
        - id
      type: object
      pattern: org.DeprivationAppointmentModel
      properties:
        id:
          description: |
            削除対象引上げ権限のID<br>
            *「引上げ権限作成API」または「引上げ権限参照API」で取得した値を指定
          type: integer
          example: 999
