content:
  application/json:
    schema:
      required:
        - id
      type: object
      pattern: org.UniversalRoleModel
      properties:
        id:
          description: |
            削除対象ユニバーサルロールのID<br>
            *「ユニバーサルロール作成API」または「ユニバーサルロール参照API」で取得した値を指定
          type: integer
          example: 999
